import { Pressable, SafeAreaView, ScrollView, StyleSheet, Text, View } from "react-native";
import type { NativeStackScreenProps } from "@react-navigation/native-stack";

import { useSettings } from "@/context/SettingsContext";
import { useTasks } from "@/context/TaskContext";
import { spacing } from "@/theme/colors";
import type { TasksStackParamList } from "@/navigation/navigationTypes";
import { getDueDateStatus } from "@/utils/validation";
import { groupTasksByDueDate } from "@/utils/productivity";

type Props = NativeStackScreenProps<TasksStackParamList, "Timeline">;

export function TimelineScreen({ navigation }: Props) {
  const { colors } = useSettings();
  const { tasks } = useTasks();
  const groups = groupTasksByDueDate(tasks);

  return <SafeAreaView style={[styles.safeArea, { backgroundColor: colors.background }]}><ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}><View style={styles.header}><Pressable accessibilityRole="button" accessibilityLabel="Go back to tasks" onPress={() => navigation.goBack()} style={({ pressed }) => [styles.backButton, { backgroundColor: colors.surface, borderColor: colors.border }, pressed && styles.pressed]}><Text style={[styles.backIcon, { color: colors.primary }]}>‹</Text></Pressable><View style={styles.headerCopy}><Text style={[styles.eyebrow, { color: colors.primary }]}>PLAN AHEAD</Text><Text style={[styles.title, { color: colors.text }]}>Deadline timeline</Text></View><View style={{ width: 42 }} /></View><View style={[styles.hero, { backgroundColor: colors.primary }]}><Text style={styles.heroTitle}>See what’s next.</Text><Text style={styles.heroText}>Your academic deadlines, arranged in one clear path.</Text><View style={styles.heroStats}><Text style={styles.heroStat}>{tasks.length}</Text><Text style={styles.heroStatLabel}>total tasks</Text><Text style={styles.heroDivider}>•</Text><Text style={styles.heroStat}>{groups.length}</Text><Text style={styles.heroStatLabel}>date groups</Text></View></View>{groups.length === 0 ? <View style={[styles.empty, { backgroundColor: colors.surface, borderColor: colors.border }]}><Text style={[styles.emptyTitle, { color: colors.text }]}>Your timeline is clear.</Text><Text style={[styles.emptyText, { color: colors.muted }]}>Add a task to see it appear here.</Text></View> : <View style={styles.timeline}>{groups.map((group, groupIndex) => <View key={group.date} style={styles.group}><View style={styles.dateRail}><View style={[styles.dateDot, { backgroundColor: colors.primary }]} />{groupIndex < groups.length - 1 ? <View style={[styles.dateLine, { backgroundColor: colors.border }]} /> : null}</View><View style={styles.groupBody}><Text style={[styles.dateLabel, { color: colors.text }]}>{group.label}</Text><Text style={[styles.dateCaption, { color: colors.muted }]}>{group.tasks.length} {group.tasks.length === 1 ? "task" : "tasks"}</Text>{group.tasks.map((task) => { const dueStatus = getDueDateStatus(task); const statusColor = dueStatus === "Overdue" ? colors.danger : dueStatus === "Completed" ? colors.success : dueStatus === "Due soon" || dueStatus === "Due today" ? colors.warning : colors.primary; return <Pressable key={task.id} onPress={() => navigation.navigate("TaskDetails", { taskId: task.id })} style={({ pressed }) => [styles.taskCard, { backgroundColor: colors.surface, borderColor: colors.border }, pressed && styles.pressed]}><View style={[styles.taskAccent, { backgroundColor: statusColor }]} /><View style={styles.taskCopy}><Text style={[styles.taskTitle, { color: colors.text }]} numberOfLines={1}>{task.title}</Text><Text style={[styles.taskSubject, { color: colors.muted }]} numberOfLines={1}>{task.subject}</Text></View><Text style={[styles.taskStatus, { color: statusColor }]}>{dueStatus}</Text></Pressable>; })}</View></View>)}</View>}</ScrollView></SafeAreaView>;
}

const styles = StyleSheet.create({
  safeArea: { flex: 1 }, content: { padding: spacing.md, paddingBottom: spacing.xl }, header: { alignItems: "center", flexDirection: "row", justifyContent: "space-between", marginBottom: spacing.lg }, backButton: { alignItems: "center", borderRadius: 21, borderWidth: 1, height: 42, justifyContent: "center", width: 42 }, backIcon: { fontSize: 31, lineHeight: 34, marginTop: -3 }, headerCopy: { flex: 1 }, eyebrow: { fontSize: 11, fontWeight: "900", letterSpacing: 1.8, textAlign: "center" }, title: { fontSize: 24, fontWeight: "900", marginTop: 4, textAlign: "center" }, hero: { borderRadius: 20, padding: spacing.lg }, heroTitle: { color: "#FFFFFF", fontSize: 23, fontWeight: "900" }, heroText: { color: "#DDE5FF", fontSize: 13, lineHeight: 19, marginTop: 6 }, heroStats: { alignItems: "baseline", flexDirection: "row", marginTop: spacing.lg }, heroStat: { color: "#FFFFFF", fontSize: 24, fontWeight: "900" }, heroStatLabel: { color: "#DDE5FF", fontSize: 11, marginLeft: 5 }, heroDivider: { color: "#AAB8FF", fontSize: 18, marginHorizontal: 12 }, timeline: { marginTop: spacing.lg }, group: { flexDirection: "row", minHeight: 112 }, dateRail: { alignItems: "center", marginRight: spacing.sm, width: 19 }, dateDot: { borderRadius: 8, height: 15, width: 15, zIndex: 1 }, dateLine: { bottom: 0, position: "absolute", top: 15, width: 2 }, groupBody: { flex: 1, paddingBottom: spacing.md }, dateLabel: { fontSize: 16, fontWeight: "900" }, dateCaption: { fontSize: 11, marginTop: 3 }, taskCard: { alignItems: "center", borderRadius: 14, borderWidth: 1, flexDirection: "row", marginTop: spacing.sm, minHeight: 58, padding: spacing.sm }, taskAccent: { borderRadius: 3, height: 35, marginRight: spacing.sm, width: 4 }, taskCopy: { flex: 1, minWidth: 0 }, taskTitle: { fontSize: 13, fontWeight: "800" }, taskSubject: { fontSize: 11, marginTop: 3 }, taskStatus: { fontSize: 10, fontWeight: "800", marginLeft: spacing.xs, maxWidth: 70, textAlign: "right" }, empty: { alignItems: "center", borderRadius: 18, borderWidth: 1, marginTop: spacing.lg, padding: spacing.xl }, emptyTitle: { fontSize: 18, fontWeight: "800" }, emptyText: { fontSize: 13, marginTop: 8 }, pressed: { opacity: 0.75, transform: [{ scale: 0.99 }] },
});
